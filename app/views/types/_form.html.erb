<%= form_with(model: type, local: true) do |form| %>
  <% if type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(type.errors.count, "error") %> prohibited this type from being saved:</h2>

      <ul>
      <% type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="container">
    <div class="form-group">
      <div class="field">
        <label for="exampleFormControlTextarea1">Libellé du type</label>
        <%= form.text_field :label , type:"text", class:"form-control" %>
      </div>

    <div class="actions">
      <%= form.submit 'Enregistrer', class: 'btn btn-primary'  %>
    </div>
<% end %>
